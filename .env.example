# Optional: where FormSubmit delivers messages (default in code is the test Gmail).
# When ready for production, set to contact@goethos.us and confirm that inbox once via FormSubmit.
NEXT_PUBLIC_FORM_RECIPIENT=saikrishnareddypoluri@gmail.com
