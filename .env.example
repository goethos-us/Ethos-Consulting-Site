# Optional: FormSubmit delivery address (must match an inbox you can confirm once in FormSubmit).
# Safe to commit — use real values only in Vercel Environment Variables or .env.local (gitignored).
NEXT_PUBLIC_FORM_RECIPIENT=contact@goethos.us
